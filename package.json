{
  "name": "website-scraper-frontend",
  "version": "1.0.0",
  "description": "Frontend for web scraping project",
  "main": "index.js",
  "scripts": "scripts": {
    "build": "esbuild src/script.js --bundle --minify --outfile=dist/script.js && cleancss -o dist/styles.css src/styles.css",
    "start": "npm run build && netlify dev"

  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  },
  "keywords": ["scraping", "frontend", "web", "vercel"],
  "author": "Your Name",
  "license": "MIT"
}
